
function onLoad(activatedWhileWindowOpen) {
  // We do not load anything anymore, but we need at least one registered
  // overlay to get the options page registered.
}

function onUnload(deactivatedWhileWindowOpen) {
}
